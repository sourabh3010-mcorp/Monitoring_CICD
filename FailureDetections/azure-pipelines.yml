trigger:
  branches:
    include:
      - main

pool:
  vmImage: ubuntu-latest

parameters:
  - name: environment
    displayName: "Environment"
    type: string
    default: "qa"
    values:
      - qa
      - prod

variables:
  environment: ${{ parameters.environment }}

steps:
  - checkout: self

  # Install Terraform
  - script: |
      sudo apt-get update -y
      sudo apt-get install -y wget unzip
      wget https://releases.hashicorp.com/terraform/1.7.5/terraform_1.7.5_linux_amd64.zip
      unzip terraform_1.7.5_linux_amd64.zip
      sudo mv terraform /usr/local/bin/
      terraform -version
    displayName: "Install Terraform 1.7.5"

  # Login to Azure using Service Connection (and export ARM_* for Terraform in the same AzureCLI task)
  - task: AzureCLI@2
    displayName: "Azure Login and export ARM_* for Terraform"
    inputs:
      azureSubscription: "VS Professional Subscription-Nitin Pradhan"
      scriptType: bash
      scriptLocation: inlineScript
      inlineScript: |
        echo "Azure Login Successful"
        az account show

        echo "Exporting ARM_* credentials for Terraform..."

        # NOTE:
        # $(servicePrincipalId) and $(servicePrincipalKey) will be expanded by Azure DevOps
        # at runtime into literal values if they exist in the service connection or pipeline variables.
        # We assign them to local shell variables here.
        SP_ID="$(servicePrincipalId)"
        SP_SECRET="$(servicePrincipalKey)"

        # Get tenant and subscription from the currently logged-in service connection
        TENANT_ID=$(az account show --query tenantId -o tsv)
        SUB_ID=$(az account show --query id -o tsv)

        # Set pipeline variables (ARM_CLIENT_ID and ARM_CLIENT_SECRET are marked secret)
        echo "##vso[task.setvariable variable=ARM_CLIENT_ID;issecret=true]$SP_ID"
        echo "##vso[task.setvariable variable=ARM_CLIENT_SECRET;issecret=true]$SP_SECRET"
        echo "##vso[task.setvariable variable=ARM_TENANT_ID]$TENANT_ID"
        echo "##vso[task.setvariable variable=ARM_SUBSCRIPTION_ID]$SUB_ID"

        echo "Terraform authentication variables exported."
  # ======================================================
  # Terraform Init
  # ======================================================
  - script: |
      terraform init \
        -backend-config="resource_group_name=tfstate-rg-monitoring" \
        -backend-config="storage_account_name=tfstatestoracct02" \
        -backend-config="container_name=tfstate" \
        -backend-config="key=$(environment).tfstate"
    workingDirectory: FailureDetections/terraform
    displayName: "Terraform Init"
    env:
      ARM_CLIENT_ID: $(ARM_CLIENT_ID)
      ARM_CLIENT_SECRET: $(ARM_CLIENT_SECRET)
      ARM_TENANT_ID: $(ARM_TENANT_ID)
      ARM_SUBSCRIPTION_ID: $(ARM_SUBSCRIPTION_ID)

  # Terraform Validat