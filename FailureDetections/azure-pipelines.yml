trigger:
  branches:
    include:
      - main

pool:
  vmImage: ubuntu-latest

parameters:
  - name: environment
    displayName: "Environment"
    type: string
    default: "qa"
    values:
      - qa
      - prod

variables:
  environment: ${{ parameters.environment }}
  # Expect these secure variables to be created in the pipeline or a variable group:
  # TF_CLIENT_ID (secret), TF_CLIENT_SECRET (secret), TF_TENANT_ID, TF_SUBSCRIPTION_ID

steps:
  - checkout: self

  # Install Terraform
  - script: |
      sudo apt-get update -y
      sudo apt-get install -y wget unzip
      wget https://releases.hashicorp.com/terraform/1.7.5/terraform_1.7.5_linux_amd64.zip
      unzip terraform_1.7.5_linux_amd64.zip
      sudo mv terraform /usr/local/bin/
      terraform -version
    displayName: "Install Terraform 1.7.5"

  # Optional: check Azure context (not used for Terraform auth when using explicit SP credentials)
  - task: AzureCLI@2
    displayName: "Azure CLI context check (optional)"
    inputs:
      azureSubscription: "VS Professional Subscription-Nitin Pradhan"
      scriptType: bash
      scriptLocation: inlineScript
      inlineScript: |
        echo "Azure CLI context (for debug only)"
        az account show

  # Terraform Init — uses explicit Service Principal credentials provided via secure pipeline variables (TF_*)
  - script: |
      echo "Initializing Terraform backend for environment: $(environment)"
      terraform init \
        -backend-config="resource_group_name=tfstate-rg-monitoring" \
        -backend-config="storage_account_name=tfstatestoracct02" \
        -backend-config="container_name=tfstate" \
        -backend-config="key=$(environment).tfstate"
    workingDirectory: FailureDetections/terraform
    displayName: "Terraform Init (using pre-provisioned SP credentials)"
    env:
      # Map secure TF_* variables into ARM_* environment variables for Terraform
      ARM_CLIENT_ID: $(TF_CLIENT_ID)
      ARM_CLIENT_SECRET: $(TF_CLIENT_SECRET)
      ARM_TENANT_ID: "c160a942-c869-429f-8a96-f8c8296d57db"
      ARM_SUBSCRIPTION_ID: $(TF_SUBSCRIPTION_ID)